import { FilterComponentsPipe } from './filter-components.pipe';

describe('FilterComponentsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterComponentsPipe();
    expect(pipe).toBeTruthy();
  });
});
